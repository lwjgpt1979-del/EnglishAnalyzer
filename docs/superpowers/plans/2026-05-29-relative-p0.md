# 亲人端 P0 三项实施计划（Plan L）

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development

**Goal:** 补齐亲人端 3 个 P0：
1. **绑定学生账号**（学生侧生成 relative_bind 邀请码 → 亲人侧扫码/输码绑定，最多 4 个亲人）
2. **查看学情数据**（学情报告 + 错题列表）
3. **代付会员**（亲人为绑定学生付钱，14-17 岁学生由亲人代付视为已获监护人同意）

依据需求文档 P0 亲人端清单 + §四D 亲人账号操作流程 + §4.1（14-17 岁亲人代付 = 监护人同意）。

**Architecture:**
- 数据层：**完全无新迁移**。
  - `invite_code_type` 已含 `relative_bind`（D-061 / 域1）
  - `StudentRelative` 表已建（域1：student_id/relative_id/relationship/is_active/bound_at/unbound_at）
  - `Relative` 表已建（仅 PK FK to users.id 的角色标记表）
  - `Order` 表的 `payer_id` 和 `beneficiary_id` 已分离 → 代付天然支持
- 服务：新 `relative_service` 管邀请码生成 / 绑定 / 列表 / 解绑；`order_service.create_order` 已支持 payer ≠ beneficiary，API 层暴露 `target_student_id` 即可。
- API：`/relative/*` 端点组（邀请码生成由学生侧调用、绑定由亲人侧调用、view 由亲人调用）；`/orders` POST 加 `target_student_id` 可选参数。
- 角色：bind 成功 → 创建 Relative 行 + user.role = "relative"（学生为零绑定可保留 student；本批简化为单角色）。
- 14-17 代付：beneficiary 是 14-17 岁学生 + payer 是其绑定亲人 → 自动写入 `minor_purchase_consent_at`，跳过 minor_consent 表单校验。

**Tech Stack:** FastAPI · SQLAlchemy 2.x asyncio · Pydantic v2 · pytest-asyncio STRICT · uni-app Vue3

---

## File Structure

```
新增后端:
  backend/app/services/relative_service.py
  backend/app/schemas/relative.py
  backend/app/api/v1/relative.py
  tests/api/test_relative.py

修改后端:
  backend/app/api/v1/router.py                  # +relative_router
  backend/app/api/v1/orders.py 或 memberships.py # POST 加 target_student_id
  backend/app/schemas/payments.py（或 orders）   # OrderCreate 加 target_student_id
  backend/app/services/order_service.py         # create_order 透传 beneficiary
  backend/app/services/membership_service.py    # 已用 order.beneficiary_id，确认

新增前端:
  frontend/miniprogram/src/api/relative.ts
  frontend/miniprogram/src/pages/relative/center.vue
  frontend/miniprogram/src/pages/relative/student-view.vue

修改前端:
  frontend/miniprogram/src/types/api.ts         # +类型
  frontend/miniprogram/src/api/orders.ts        # createOrder 加 target_student_id
  frontend/miniprogram/src/pages.json           # +2 页
  frontend/miniprogram/src/pages/profile/index.vue # 加"亲人中心"入口 + "我的家人/邀请家人"
```

**Key facts:**
- 最多 4 个 active relatives 每学生（service 层校验）
- `relationship` 字符串自由填（母亲/父亲/祖父/外婆/...）
- 亲人解绑：set `is_active=false`, `unbound_at=now()`（保留历史）

---

## Task 0: Relative service + schemas + 绑定流程 + 测试

**Files:**
- Create: `backend/app/schemas/relative.py`
- Create: `backend/app/services/relative_service.py`
- Create: `backend/app/api/v1/relative.py`
- Modify: `backend/app/api/v1/router.py`
- Create: `tests/api/test_relative.py`

- [ ] **Step 1: 创建 `backend/app/schemas/relative.py`**

```python
"""亲人端 Schemas（D-076 / P0 亲人端）。"""
from __future__ import annotations

import uuid
from datetime import datetime
from pydantic import BaseModel, Field


class RelativeInviteCodeOut(BaseModel):
    code: str
    expires_at: datetime


class BindRelativeRequest(BaseModel):
    code: str = Field(..., min_length=6, max_length=6)
    relationship: str = Field(..., min_length=1, max_length=16, description="如：母亲/父亲/祖父/外婆")


class StudentRelativeOut(BaseModel):
    id: uuid.UUID
    student_id: uuid.UUID
    relative_id: uuid.UUID
    relationship: str
    is_active: bool
    bound_at: datetime

    model_config = {"from_attributes": True}


class BoundStudentOut(BaseModel):
    """亲人视角的绑定学生条目。"""
    student_id: uuid.UUID
    relationship: str
    bound_at: datetime
```

- [ ] **Step 2: 创建 `backend/app/services/relative_service.py`**

```python
"""亲人端业务逻辑（D-076 / P0 亲人端）。

功能：
- generate_invite_code: 学生侧生成 relative_bind 邀请码（有效24h）
- bind_relative: 亲人侧通过邀请码绑定到学生（最多4个 active 亲人 + 创建 Relative 行）
- get_my_students: 亲人查看绑定的所有学生
- get_my_relatives: 学生查看绑定的所有亲人
- unbind_relative: 学生侧解绑某个亲人（set is_active=false）
"""
from __future__ import annotations

import random
import string
import uuid
from datetime import datetime, timedelta, timezone

from sqlalchemy import select, func
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.exceptions import AppError
from app.models.d1_users import InviteCode, Relative, StudentRelative, User

_CODE_CHARS = string.ascii_uppercase + string.digits
_CODE_LENGTH = 6
_CODE_TTL_HOURS = 24
MAX_RELATIVES_PER_STUDENT = 4


async def generate_invite_code(
    db: AsyncSession,
    *,
    student_id: uuid.UUID,
) -> InviteCode:
    """学生为自己生成 relative_bind 邀请码。"""
    async def _unique_code() -> str:
        for _ in range(10):
            code = "".join(random.choices(_CODE_CHARS, k=_CODE_LENGTH))
            r = await db.execute(select(InviteCode).where(InviteCode.code == code))
            if r.scalar_one_or_none() is None:
                return code
        raise AppError(code=500, message="邀请码生成失败，请重试")

    code = await _unique_code()
    invite = InviteCode(
        id=uuid.uuid4(),
        code=code,
        type="relative_bind",  # type: ignore[arg-type]
        issuer_id=student_id,
        target_id=None,
        expires_at=datetime.now(timezone.utc) + timedelta(hours=_CODE_TTL_HOURS),
    )
    db.add(invite)
    await db.flush()
    return invite


async def bind_relative(
    db: AsyncSession,
    *,
    relative_user: User,
    code: str,
    relationship: str,
) -> StudentRelative:
    """亲人扫码绑定到学生。

    - 码无效/过期/已用 → 400
    - 已与该学生有 active 绑定 → 409
    - 该学生已有 4 个 active 亲人 → 400
    - 成功 → 创建 Relative 行（如无）+ StudentRelative 行；user.role 升级为 'relative'
    """
    now = datetime.now(timezone.utc)

    invite_r = await db.execute(
        select(InviteCode).where(
            InviteCode.code == code,
            InviteCode.type == "relative_bind",
            InviteCode.used_at.is_(None),
            InviteCode.expires_at > now,
        )
    )
    invite = invite_r.scalar_one_or_none()
    if invite is None:
        raise AppError(code=400, message="邀请码无效或已过期")

    student_id = invite.issuer_id

    # 不能绑定到自己
    if student_id == relative_user.id:
        raise AppError(code=400, message="不能绑定到自己")

    # 已 active 绑定 → 409
    existing_r = await db.execute(
        select(StudentRelative).where(
            StudentRelative.student_id == student_id,
            StudentRelative.relative_id == relative_user.id,
            StudentRelative.is_active.is_(True),
        )
    )
    if existing_r.scalar_one_or_none() is not None:
        raise AppError(code=409, message="您已是该学生的家人")

    # 该学生 active 亲人数已达上限 → 400
    cnt_r = await db.execute(
        select(func.count(StudentRelative.id)).where(
            StudentRelative.student_id == student_id,
            StudentRelative.is_active.is_(True),
        )
    )
    if cnt_r.scalar_one() >= MAX_RELATIVES_PER_STUDENT:
        raise AppError(code=400, message=f"该学生已有 {MAX_RELATIVES_PER_STUDENT} 个家人，达到上限")

    # 确保 Relative 行存在
    rel_r = await db.execute(select(Relative).where(Relative.id == relative_user.id))
    if rel_r.scalar_one_or_none() is None:
        db.add(Relative(id=relative_user.id))

    # 升级 role 为 relative
    relative_user.role = "relative"  # type: ignore[assignment]

    sr = StudentRelative(
        id=uuid.uuid4(),
        student_id=student_id,
        relative_id=relative_user.id,
        relationship=relationship,
        is_active=True,
        bound_at=now,
    )
    db.add(sr)
    invite.used_at = now
    await db.flush()
    return sr


async def get_my_students(
    db: AsyncSession,
    *,
    relative_id: uuid.UUID,
) -> list[StudentRelative]:
    r = await db.execute(
        select(StudentRelative).where(
            StudentRelative.relative_id == relative_id,
            StudentRelative.is_active.is_(True),
        ).order_by(StudentRelative.bound_at.desc())
    )
    return list(r.scalars().all())


async def get_my_relatives(
    db: AsyncSession,
    *,
    student_id: uuid.UUID,
) -> list[StudentRelative]:
    r = await db.execute(
        select(StudentRelative).where(
            StudentRelative.student_id == student_id,
            StudentRelative.is_active.is_(True),
        ).order_by(StudentRelative.bound_at.desc())
    )
    return list(r.scalars().all())


async def unbind_relative(
    db: AsyncSession,
    *,
    student_id: uuid.UUID,
    relative_id: uuid.UUID,
) -> None:
    """学生解绑某个亲人。"""
    r = await db.execute(
        select(StudentRelative).where(
            StudentRelative.student_id == student_id,
            StudentRelative.relative_id == relative_id,
            StudentRelative.is_active.is_(True),
        )
    )
    sr = r.scalar_one_or_none()
    if sr is None:
        raise AppError(code=404, message="未找到该亲人绑定关系")
    sr.is_active = False
    sr.unbound_at = datetime.now(timezone.utc)
    await db.flush()


async def assert_bound(
    db: AsyncSession,
    *,
    relative_id: uuid.UUID,
    student_id: uuid.UUID,
) -> None:
    """权限 helper：亲人查看/操作学生前调用。"""
    r = await db.execute(
        select(StudentRelative).where(
            StudentRelative.student_id == student_id,
            StudentRelative.relative_id == relative_id,
            StudentRelative.is_active.is_(True),
        )
    )
    if r.scalar_one_or_none() is None:
        raise AppError(code=403, message="您不是该学生的家人")
```

- [ ] **Step 3: 创建 `backend/app/api/v1/relative.py`**

```python
"""亲人端 API（D-076 / P0 亲人端）。"""
from __future__ import annotations

import uuid
from typing import Annotated

from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import get_db, get_rls_db
from app.core.exceptions import AppError
from app.core.security import get_current_user
from app.models.d1_users import User
from app.schemas.base import BaseResponse, make_ok
from app.schemas.relative import (
    BindRelativeRequest,
    BoundStudentOut,
    RelativeInviteCodeOut,
    StudentRelativeOut,
)
from app.services import relative_service

router = APIRouter(prefix="/relative", tags=["relative"])

DbDep = Annotated[AsyncSession, Depends(get_db)]
UserDep = Annotated[User, Depends(get_current_user)]


@router.post("/invite-code", response_model=BaseResponse[RelativeInviteCodeOut])
async def generate_relative_invite_code(db: DbDep, current_user: UserDep):
    """学生生成 relative_bind 邀请码（任何登录用户都可生成自己的家人邀请码）。"""
    await get_rls_db(db, str(current_user.id))
    invite = await relative_service.generate_invite_code(db, student_id=current_user.id)
    await db.commit()
    return make_ok(RelativeInviteCodeOut(code=invite.code, expires_at=invite.expires_at))


@router.post("/bind", response_model=BaseResponse[StudentRelativeOut])
async def bind_as_relative(body: BindRelativeRequest, db: DbDep, current_user: UserDep):
    """亲人通过邀请码绑定到学生。"""
    await get_rls_db(db, str(current_user.id))
    sr = await relative_service.bind_relative(
        db, relative_user=current_user, code=body.code.upper(), relationship=body.relationship,
    )
    await db.commit()
    return make_ok(StudentRelativeOut(
        id=sr.id, student_id=sr.student_id, relative_id=sr.relative_id,
        relationship=sr.relationship, is_active=sr.is_active, bound_at=sr.bound_at,
    ))


@router.get("/students", response_model=BaseResponse[list[BoundStudentOut]])
async def list_my_students(db: DbDep, current_user: UserDep):
    """亲人查看绑定的所有学生。"""
    await get_rls_db(db, str(current_user.id))
    items = await relative_service.get_my_students(db, relative_id=current_user.id)
    return make_ok([
        BoundStudentOut(student_id=sr.student_id, relationship=sr.relationship, bound_at=sr.bound_at)
        for sr in items
    ])


@router.get("/my-relatives", response_model=BaseResponse[list[BoundStudentOut]])
async def list_my_relatives(db: DbDep, current_user: UserDep):
    """学生查看自己绑定的所有亲人。"""
    await get_rls_db(db, str(current_user.id))
    items = await relative_service.get_my_relatives(db, student_id=current_user.id)
    return make_ok([
        BoundStudentOut(student_id=sr.relative_id, relationship=sr.relationship, bound_at=sr.bound_at)
        for sr in items
    ])  # student_id 字段在此场景复用为 relative_id


@router.delete("/relatives/{relative_id}", response_model=BaseResponse[dict])
async def unbind_my_relative(relative_id: uuid.UUID, db: DbDep, current_user: UserDep):
    """学生解绑某个亲人。"""
    await get_rls_db(db, str(current_user.id))
    await relative_service.unbind_relative(
        db, student_id=current_user.id, relative_id=relative_id,
    )
    await db.commit()
    return make_ok({"unbound": True})
```

- [ ] **Step 4: 注册 router**

READ `backend/app/api/v1/router.py`，加 import + include：
```python
from app.api.v1.relative import router as relative_router
v1_router.include_router(relative_router)
```

- [ ] **Step 5: 创建 `tests/api/test_relative.py`**

```python
"""亲人端测试（D-076）。"""
import uuid
import pytest
import pytest_asyncio
from unittest.mock import AsyncMock, patch
from httpx import AsyncClient, ASGITransport
from sqlalchemy import select

from app.main import app
from app.core.database import _async_session_factory
from app.models.d1_users import User


@pytest_asyncio.fixture
async def client():
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://testserver") as ac:
        yield ac


async def _login(client: AsyncClient, suffix: str) -> dict:
    with patch("app.services.auth_service.wechat_code2session", new_callable=AsyncMock) as mock_wx:
        mock_wx.return_value = {"openid": f"rel_{suffix}"}
        resp = await client.post("/api/v1/auth/wx-login", json={"code": "test"})
    return {"Authorization": f"Bearer {resp.json()['data']['access_token']}"}


async def _setup_student(client: AsyncClient, suffix: str, birth_year: int = 2010) -> tuple[dict, str]:
    """注册一个学生并完善 profile。返回 (headers, student_id)。"""
    headers = await _login(client, suffix)
    if birth_year < 2010:  # >=14 不需要 guardian
        await client.post(
            "/api/v1/auth/complete-profile",
            json={"birth_year": birth_year, "agreement_version": "v1.0"}, headers=headers,
        )
    else:  # <14 需要 guardian flow
        await client.post(
            "/api/v1/auth/complete-profile",
            json={"birth_year": birth_year, "guardian_phone": "13800001234", "agreement_version": "v1.0"},
            headers=headers,
        )
        await client.post("/api/v1/auth/guardian-verify", json={"code": "123456"}, headers=headers)
    async with _async_session_factory() as s:
        user = (await s.execute(
            select(User).where(User.openid == f"rel_{suffix}")
        )).scalar_one()
        return headers, str(user.id)


@pytest.mark.asyncio
async def test_student_generates_relative_invite_code(client):
    s_headers, _ = await _setup_student(client, f"genv_{uuid.uuid4().hex[:6]}", 1990)
    r = await client.post("/api/v1/relative/invite-code", headers=s_headers)
    assert r.status_code == 200
    assert len(r.json()["data"]["code"]) == 6


@pytest.mark.asyncio
async def test_full_bind_flow(client):
    """学生生成码 → 亲人绑定 → 双方列表均能看到。"""
    s_headers, sid = await _setup_student(client, f"bind_s_{uuid.uuid4().hex[:6]}", 2012)
    p_headers, pid = await _setup_student(client, f"bind_p_{uuid.uuid4().hex[:6]}", 1985)

    iv = await client.post("/api/v1/relative/invite-code", headers=s_headers)
    code = iv.json()["data"]["code"]

    r = await client.post(
        "/api/v1/relative/bind",
        json={"code": code, "relationship": "母亲"}, headers=p_headers,
    )
    assert r.status_code == 200
    assert r.json()["data"]["relationship"] == "母亲"
    assert r.json()["data"]["student_id"] == sid

    # 亲人侧能看到学生
    r2 = await client.get("/api/v1/relative/students", headers=p_headers)
    assert r2.json()["data"][0]["student_id"] == sid

    # 学生侧能看到亲人
    r3 = await client.get("/api/v1/relative/my-relatives", headers=s_headers)
    assert any(item["student_id"] == pid for item in r3.json()["data"])  # 注：API 复用 student_id 字段表示 relative_id


@pytest.mark.asyncio
async def test_invalid_code_400(client):
    _, _ = await _setup_student(client, f"badcode_{uuid.uuid4().hex[:6]}", 1985)
    headers = await _login(client, f"badcode_p_{uuid.uuid4().hex[:6]}")
    await client.post(
        "/api/v1/auth/complete-profile",
        json={"birth_year": 1985, "agreement_version": "v1.0"}, headers=headers,
    )
    r = await client.post(
        "/api/v1/relative/bind",
        json={"code": "XXXXXX", "relationship": "父亲"}, headers=headers,
    )
    assert r.status_code == 400


@pytest.mark.asyncio
async def test_duplicate_bind_409(client):
    s_headers, _ = await _setup_student(client, f"dup_s_{uuid.uuid4().hex[:6]}", 2010)
    p_headers, _ = await _setup_student(client, f"dup_p_{uuid.uuid4().hex[:6]}", 1985)

    iv1 = await client.post("/api/v1/relative/invite-code", headers=s_headers)
    await client.post(
        "/api/v1/relative/bind",
        json={"code": iv1.json()["data"]["code"], "relationship": "母亲"}, headers=p_headers,
    )
    iv2 = await client.post("/api/v1/relative/invite-code", headers=s_headers)
    r = await client.post(
        "/api/v1/relative/bind",
        json={"code": iv2.json()["data"]["code"], "relationship": "母亲"}, headers=p_headers,
    )
    assert r.status_code == 409


@pytest.mark.asyncio
async def test_max_4_relatives_limit(client):
    """学生超过 4 个 active 亲人时第 5 个绑定失败。"""
    s_headers, _ = await _setup_student(client, f"max_s_{uuid.uuid4().hex[:6]}", 2010)
    # 前 4 个亲人成功
    for i in range(4):
        p_h = await _login(client, f"max_p{i}_{uuid.uuid4().hex[:6]}")
        await client.post(
            "/api/v1/auth/complete-profile",
            json={"birth_year": 1985, "agreement_version": "v1.0"}, headers=p_h,
        )
        iv = await client.post("/api/v1/relative/invite-code", headers=s_headers)
        await client.post(
            "/api/v1/relative/bind",
            json={"code": iv.json()["data"]["code"], "relationship": f"亲人{i}"}, headers=p_h,
        )
    # 第 5 个失败
    p5 = await _login(client, f"max_p5_{uuid.uuid4().hex[:6]}")
    await client.post(
        "/api/v1/auth/complete-profile",
        json={"birth_year": 1985, "agreement_version": "v1.0"}, headers=p5,
    )
    iv5 = await client.post("/api/v1/relative/invite-code", headers=s_headers)
    r = await client.post(
        "/api/v1/relative/bind",
        json={"code": iv5.json()["data"]["code"], "relationship": "第五个"}, headers=p5,
    )
    assert r.status_code == 400


@pytest.mark.asyncio
async def test_unbind_relative(client):
    """学生解绑亲人。"""
    s_headers, _ = await _setup_student(client, f"ub_s_{uuid.uuid4().hex[:6]}", 2010)
    p_headers, pid = await _setup_student(client, f"ub_p_{uuid.uuid4().hex[:6]}", 1985)

    iv = await client.post("/api/v1/relative/invite-code", headers=s_headers)
    await client.post(
        "/api/v1/relative/bind",
        json={"code": iv.json()["data"]["code"], "relationship": "母亲"}, headers=p_headers,
    )

    r = await client.delete(f"/api/v1/relative/relatives/{pid}", headers=s_headers)
    assert r.status_code == 200

    r2 = await client.get("/api/v1/relative/my-relatives", headers=s_headers)
    assert all(item["student_id"] != pid for item in r2.json()["data"])
```

- [ ] **Step 6: 跑测试 + 全量**

```bash
cd /Users/johnlu/Desktop/ComeMoney/项目/ai-education/engGramer/backend
python -m pytest ../tests/api/test_relative.py -v
python -m pytest ../tests/ -q
```
Expected: test_relative 6 PASS；全量 212 PASS（206 + 6）。

- [ ] **Step 7: 提交**

```bash
cd /Users/johnlu/Desktop/ComeMoney/项目/ai-education/engGramer
git add backend/app/schemas/relative.py backend/app/services/relative_service.py \
        backend/app/api/v1/relative.py backend/app/api/v1/router.py tests/api/test_relative.py
git commit -m "feat(relative): invite-code bind + list + unbind + max-4 limit"
```

---

## Task 1: 亲人查看学生学情/错题 + 测试

**Files:**
- Modify: `backend/app/api/v1/relative.py`
- Modify: `tests/api/test_relative.py`

- [ ] **Step 1: relative.py 加 2 个 view 端点**

```python
from app.schemas.diagnosis import DiagnosisReport
from app.schemas.wrong_questions import WrongQuestionOut


@router.get(
    "/students/{student_id}/diagnosis-report",
    response_model=BaseResponse[DiagnosisReport],
)
async def relative_view_student_diagnosis(
    student_id: uuid.UUID, db: DbDep, current_user: UserDep,
):
    await get_rls_db(db, str(current_user.id))
    await relative_service.assert_bound(
        db, relative_id=current_user.id, student_id=student_id,
    )
    from app.services.diagnosis_service import get_diagnosis_report
    report = await get_diagnosis_report(db, student_id=student_id)
    return make_ok(report)


@router.get(
    "/students/{student_id}/wrong-questions",
    response_model=BaseResponse[list[WrongQuestionOut]],
)
async def relative_view_student_wqs(
    student_id: uuid.UUID, db: DbDep, current_user: UserDep,
):
    await get_rls_db(db, str(current_user.id))
    await relative_service.assert_bound(
        db, relative_id=current_user.id, student_id=student_id,
    )
    from sqlalchemy import select as _sel
    from app.models.d3_wrong_questions import WrongQuestion
    r = await db.execute(
        _sel(WrongQuestion).where(WrongQuestion.student_id == student_id)
        .order_by(WrongQuestion.created_at.desc())
    )
    items = list(r.scalars().all())
    return make_ok([WrongQuestionOut.model_validate(wq) for wq in items])
```

- [ ] **Step 2: 追加测试**

```python


@pytest.mark.asyncio
async def test_relative_view_student_diagnosis(client):
    s_headers, sid = await _setup_student(client, f"vd_s_{uuid.uuid4().hex[:6]}", 2010)
    p_headers, _ = await _setup_student(client, f"vd_p_{uuid.uuid4().hex[:6]}", 1985)

    iv = await client.post("/api/v1/relative/invite-code", headers=s_headers)
    await client.post(
        "/api/v1/relative/bind",
        json={"code": iv.json()["data"]["code"], "relationship": "父亲"}, headers=p_headers,
    )

    r = await client.get(f"/api/v1/relative/students/{sid}/diagnosis-report", headers=p_headers)
    assert r.status_code == 200
    d = r.json()["data"]
    assert "total_questions" in d
    assert "mastery_rate" in d


@pytest.mark.asyncio
async def test_relative_view_unbound_student_403(client):
    p_headers, _ = await _setup_student(client, f"vd_ub_{uuid.uuid4().hex[:6]}", 1985)
    rnd_sid = uuid.uuid4()
    r = await client.get(f"/api/v1/relative/students/{rnd_sid}/diagnosis-report", headers=p_headers)
    assert r.status_code == 403
```

- [ ] **Step 3: 跑测试 + 提交**

```bash
python -m pytest ../tests/api/test_relative.py -v
python -m pytest ../tests/ -q
git add backend/app/api/v1/relative.py tests/api/test_relative.py
git commit -m "feat(relative): view student diagnosis + wrong-questions with binding check"
```

---

## Task 2: 代付会员（orders 加 target_student_id 入参）

**Files:**
- Modify: `backend/app/schemas/payments.py`（OrderCreate）
- Modify: `backend/app/api/v1/orders.py`
- Modify: `backend/app/services/order_service.py`（如需）
- Modify: `tests/api/test_relative.py`

- [ ] **Step 1: schemas/payments.py 改 OrderCreate**

READ 现有 OrderCreate（应有 tier/duration_months/order_type/minor_consent 字段），追加：
```python
    target_student_id: uuid.UUID | None = Field(None, description="代付时指定学生 ID；为空则为本人购买")
```
（确保已 `import uuid` 和 `from pydantic import Field`）

- [ ] **Step 2: orders.py 端点改造**

READ `backend/app/api/v1/orders.py` 找 `create_order` 端点。当前应是 payer=beneficiary=current_user.id。改为：

a) 解析 `body.target_student_id`：
```python
beneficiary_id = body.target_student_id or current_user.id
```

b) 如果 target_student_id 非空且不等于 current_user.id：
```python
if body.target_student_id and body.target_student_id != current_user.id:
    # 代付场景：必须是该学生的 active 亲人
    from app.services import relative_service
    await relative_service.assert_bound(
        db, relative_id=current_user.id, student_id=body.target_student_id,
    )
    # 14-17 岁亲人代付视为已获监护人同意（D-073 §4.1）
    from sqlalchemy import select as _sel
    from app.models.d1_users import User as _U
    from datetime import datetime, timezone
    bu_r = await db.execute(_sel(_U).where(_U.id == body.target_student_id))
    beneficiary = bu_r.scalar_one_or_none()
    if beneficiary and beneficiary.birth_year:
        age = datetime.now(timezone.utc).year - beneficiary.birth_year
        if 14 <= age <= 17 and beneficiary.minor_purchase_consent_at is None:
            beneficiary.minor_purchase_consent_at = datetime.now(timezone.utc)
else:
    # 本人购买的 14-17 岁 minor_consent 校验保持原逻辑
    # （原 code 中调用 is_minor_14_to_17 等，保留即可）
    pass
```

c) `create_order` 调用改 payer/beneficiary：
```python
order = await order_service.create_order(
    db, payer_id=current_user.id, beneficiary_id=beneficiary_id,
    tier=body.tier, duration_months=body.duration_months, order_type=body.order_type,
)
```

确保 order_service.create_order 签名一致（前文已确认接受 payer_id/beneficiary_id 分离参数）。

- [ ] **Step 3: 校验 membership_service.activate_membership 用 order.beneficiary_id**

READ activate_membership，确认 membership.user_id 取自 order.beneficiary_id（应已如此，因为模型分离时就是这样设计）。如果原代码用的是 order.payer_id 来激活会员，**改为 order.beneficiary_id**。如果已是 beneficiary_id 则不动。

- [ ] **Step 4: 追加测试**

```python


@pytest.mark.asyncio
async def test_relative_pays_for_student(client):
    """亲人代付：order 创建 + beneficiary_id 是学生。"""
    s_headers, sid = await _setup_student(client, f"pay_s_{uuid.uuid4().hex[:6]}", 2012)  # 14岁
    p_headers, _ = await _setup_student(client, f"pay_p_{uuid.uuid4().hex[:6]}", 1985)

    iv = await client.post("/api/v1/relative/invite-code", headers=s_headers)
    await client.post(
        "/api/v1/relative/bind",
        json={"code": iv.json()["data"]["code"], "relationship": "母亲"}, headers=p_headers,
    )

    r = await client.post(
        "/api/v1/orders",
        json={
            "tier": "basic", "duration_months": 1, "order_type": "new",
            "target_student_id": sid,
        },
        headers=p_headers,
    )
    assert r.status_code == 200
    data = r.json()["data"]
    assert "id" in data
    # 14-17 岁学生 minor_purchase_consent_at 应已写入
    async with _async_session_factory() as s:
        student = (await s.execute(select(User).where(User.id == uuid.UUID(sid)))).scalar_one()
        assert student.minor_purchase_consent_at is not None


@pytest.mark.asyncio
async def test_pay_for_unbound_student_403(client):
    p_headers, _ = await _setup_student(client, f"upay_p_{uuid.uuid4().hex[:6]}", 1985)
    rnd_sid = str(uuid.uuid4())
    r = await client.post(
        "/api/v1/orders",
        json={
            "tier": "basic", "duration_months": 1, "order_type": "new",
            "target_student_id": rnd_sid,
        },
        headers=p_headers,
    )
    assert r.status_code == 403
```

- [ ] **Step 5: 跑测试 + 提交**

```bash
python -m pytest ../tests/api/test_relative.py -v
python -m pytest ../tests/ -q
git add backend/app/schemas/payments.py backend/app/api/v1/orders.py \
        backend/app/services/order_service.py 2>/dev/null \
        backend/app/services/membership_service.py 2>/dev/null \
        tests/api/test_relative.py
git commit -m "feat(orders): relative-pay support via target_student_id + minor consent bypass"
```

---

## Task 3: 前端 — 亲人中心页 + 学生侧"邀请家人" + 学生查看页

**Files:**
- Modify: `frontend/miniprogram/src/types/api.ts`
- Create: `frontend/miniprogram/src/api/relative.ts`
- Modify: `frontend/miniprogram/src/api/orders.ts`（createOrder 加 target_student_id）
- Modify: `frontend/miniprogram/src/pages.json`（+2 页）
- Create: `frontend/miniprogram/src/pages/relative/center.vue`
- Create: `frontend/miniprogram/src/pages/relative/student-view.vue`
- Modify: `frontend/miniprogram/src/pages/profile/index.vue`（加"亲人中心"入口 + "邀请家人"入口）

- [ ] **Step 1: types/api.ts 加类型**

```typescript
export interface RelativeInviteCodeOut {
  code: string
  expires_at: string
}

export interface BoundStudent {
  student_id: string  // 既用于学生 ID，也复用为 my-relatives 接口中的亲人 ID
  relationship: string
  bound_at: string
}
```

`OrderCreateRequest` 加可选 `target_student_id?: string`。

- [ ] **Step 2: api/relative.ts**

```typescript
import { request } from '@/utils/request'
import type { BaseResponse, RelativeInviteCodeOut, BoundStudent } from '../types/api'

export function generateRelativeInviteCode(): Promise<BaseResponse<RelativeInviteCodeOut>> {
  return request('/api/v1/relative/invite-code', { method: 'POST' })
}
export function bindRelative(code: string, relationship: string): Promise<BaseResponse<any>> {
  return request('/api/v1/relative/bind', { method: 'POST', data: { code, relationship } })
}
export function getMyStudents(): Promise<BaseResponse<BoundStudent[]>> {
  return request('/api/v1/relative/students', { method: 'GET' })
}
export function getMyRelatives(): Promise<BaseResponse<BoundStudent[]>> {
  return request('/api/v1/relative/my-relatives', { method: 'GET' })
}
export function unbindRelative(relativeId: string): Promise<BaseResponse<{ unbound: boolean }>> {
  return request(`/api/v1/relative/relatives/${relativeId}`, { method: 'DELETE' })
}
export function getStudentDiagnosisAsRelative(studentId: string): Promise<BaseResponse<any>> {
  return request(`/api/v1/relative/students/${studentId}/diagnosis-report`, { method: 'GET' })
}
```
（API 前缀风格按现有项目约定；若 request util 自动加 /api/v1 则去掉这里的 /api/v1/）

- [ ] **Step 3: api/orders.ts**

READ 现有 createOrder 签名，加 `target_student_id?: string` 字段透传。

- [ ] **Step 4: pages.json 加 2 页**

```json
{ "path": "pages/relative/center", "style": { "navigationBarTitleText": "家人中心" } },
{ "path": "pages/relative/student-view", "style": { "navigationBarTitleText": "孩子学情" } }
```

- [ ] **Step 5: 创建 `pages/relative/center.vue`**

```vue
<template>
  <view class="page">
    <view class="card">
      <view class="card-title">绑定孩子账号</view>
      <input v-model="bindCode" class="input" maxlength="6" placeholder="6位邀请码" @input="bindCode = bindCode.toUpperCase()" />
      <input v-model="relationship" class="input" placeholder="关系（如：母亲/父亲/祖父）" />
      <button class="btn-primary" :disabled="!bindCode || !relationship || binding" @tap="onBind">
        {{ binding ? '绑定中…' : '绑定' }}
      </button>
    </view>

    <view class="card">
      <view class="card-title">我的孩子（{{ children.length }}）</view>
      <view v-if="loading" class="tip">加载中…</view>
      <view v-else-if="children.length === 0" class="tip">还没有绑定的孩子。</view>
      <view v-for="c in children" :key="c.student_id" class="child-row" @tap="goView(c.student_id)">
        <view>
          <text class="cname">孩子 {{ c.student_id.slice(0, 8) }}…</text>
          <text class="crel">关系：{{ c.relationship }}</text>
        </view>
        <text class="arrow">›</text>
      </view>
    </view>
  </view>
</template>
<script setup lang="ts">
import { onMounted, ref } from 'vue'
import { bindRelative, getMyStudents } from '@/api/relative'
import type { BoundStudent } from '@/types/api'
const bindCode = ref('')
const relationship = ref('')
const binding = ref(false)
const children = ref<BoundStudent[]>([])
const loading = ref(false)
async function load() {
  loading.value = true
  try { const r = await getMyStudents(); children.value = r.data || [] }
  finally { loading.value = false }
}
async function onBind() {
  binding.value = true
  try {
    await bindRelative(bindCode.value, relationship.value)
    bindCode.value = ''; relationship.value = ''
    await load()
    uni.showToast({ title: '绑定成功', icon: 'success' })
  } catch (e: any) {
    uni.showToast({ title: e?.message || '绑定失败', icon: 'none' })
  } finally { binding.value = false }
}
function goView(sid: string) { uni.navigateTo({ url: `/pages/relative/student-view?studentId=${sid}` }) }
onMounted(load)
</script>
<style scoped>
.page { padding: 16rpx; background: var(--c-bg-page); min-height: 100vh; }
.card { background: var(--c-bg-card); border-radius: var(--r-lg); padding: var(--sp-4); margin-bottom: 16rpx; box-shadow: 0 4rpx 24rpx rgba(0,0,0,.04); }
.card-title { font-size: var(--fs-h2); font-weight: 700; color: var(--c-ink); margin-bottom: 16rpx; }
.input { border: 2rpx solid var(--c-border); border-radius: var(--r-md); padding: 16rpx; font-size: 28rpx; margin-bottom: 16rpx; width: 100%; box-sizing: border-box; }
.btn-primary { background: var(--c-primary); color: var(--c-ink); border-radius: var(--r-btn); padding: 20rpx; font-weight: 700; font-size: 28rpx; }
.btn-primary[disabled] { background: var(--c-primary-soft); color: #b9a94e; }
.tip { text-align: center; padding: 60rpx 0; color: var(--c-text-hint); font-size: 26rpx; }
.child-row { display: flex; justify-content: space-between; align-items: center; padding: 16rpx 0; border-bottom: 1rpx solid var(--c-border); }
.child-row:last-child { border-bottom: none; }
.cname { font-size: 28rpx; color: var(--c-ink); font-weight: 700; display: block; }
.crel { font-size: 24rpx; color: var(--c-text-hint); }
.arrow { font-size: 32rpx; color: var(--c-text-hint); }
</style>
```

- [ ] **Step 6: 创建 `pages/relative/student-view.vue`**

亲人查看某孩子的学情 + 代付入口：

```vue
<template>
  <view class="page">
    <view v-if="loading" class="tip">加载中…</view>
    <view v-else-if="!report" class="tip">暂无数据</view>
    <view v-else>
      <view class="card">
        <view class="stat-row">
          <view class="stat"><text class="num">{{ report.total_questions }}</text><text class="lbl">累计错题</text></view>
          <view class="stat"><text class="num">{{ report.total_analyzed }}</text><text class="lbl">已分析</text></view>
          <view class="stat"><text class="num">{{ Math.round(report.mastery_rate * 100) }}%</text><text class="lbl">掌握率</text></view>
        </view>
      </view>

      <view v-if="report.top_error_types.length" class="card">
        <view class="card-title">高频错误</view>
        <view v-for="e in report.top_error_types.slice(0, 5)" :key="e.error_type" class="row">
          <text>{{ e.error_type }}</text><text class="count">{{ e.count }}</text>
        </view>
      </view>

      <view v-if="report.top_suggestions?.length" class="card">
        <view class="card-title">AI 学习建议</view>
        <view v-for="(s, i) in report.top_suggestions" :key="i" class="sug">
          <text class="sug-num">{{ i + 1 }}</text>
          <text class="sug-text">{{ s }}</text>
        </view>
      </view>

      <view class="card">
        <view class="card-title">为孩子续费 / 升级会员</view>
        <view class="tier-row">
          <text v-for="t in tiers" :key="t.tier" class="tier" :class="{ active: selectedTier === t.tier }" @tap="selectedTier = t.tier">{{ t.label }}<br>¥{{ t.price }}/月</text>
        </view>
        <button class="btn-primary" :disabled="paying" @tap="onPay">{{ paying ? '支付中…' : `代付 ¥${currentPrice}` }}</button>
      </view>
    </view>
  </view>
</template>
<script setup lang="ts">
import { onMounted, ref, computed } from 'vue'
import { getStudentDiagnosisAsRelative } from '@/api/relative'
import { createOrder, payOrder } from '@/api/orders'

const studentId = ref('')
const report = ref<any>(null)
const loading = ref(true)
const paying = ref(false)
const selectedTier = ref('basic')
const tiers = [
  { tier: 'basic', label: '基础版', price: 9 },
  { tier: 'pro', label: '专业版', price: 19 },
  { tier: 'promax', label: '旗舰版', price: 39 },
]
const currentPrice = computed(() => tiers.find(t => t.tier === selectedTier.value)?.price || 0)

onMounted(async () => {
  const pages = getCurrentPages()
  studentId.value = (pages[pages.length - 1] as any).options?.studentId || ''
  if (!studentId.value) { loading.value = false; return }
  try {
    const r = await getStudentDiagnosisAsRelative(studentId.value)
    report.value = r.data
  } finally { loading.value = false }
})

async function onPay() {
  paying.value = true
  try {
    const order: any = await createOrder({
      tier: selectedTier.value, duration_months: 1, order_type: 'new',
      target_student_id: studentId.value,
    } as any)
    const params: any = await payOrder(order.id)
    await new Promise<void>((resolve, reject) => {
      wx.requestPayment({
        timeStamp: params.timeStamp, nonceStr: params.nonceStr,
        package: params.package, signType: params.signType as 'RSA' | 'MD5',
        paySign: params.paySign,
        success: () => resolve(), fail: (err) => reject(new Error(err.errMsg || '支付取消')),
      })
    })
    uni.showToast({ title: '代付成功！', icon: 'success' })
  } catch (e: any) {
    const msg = e?.message || ''
    if (msg && !msg.includes('cancel')) uni.showToast({ title: msg || '支付失败', icon: 'error' })
  } finally { paying.value = false }
}
</script>
<style scoped>
.page { padding: 24rpx; background: var(--c-bg-page); min-height: 100vh; }
.tip { text-align: center; padding: 120rpx 0; color: var(--c-text-hint); }
.card { background: var(--c-bg-card); border-radius: var(--r-lg); padding: var(--sp-4); margin-bottom: 20rpx; box-shadow: 0 4rpx 24rpx rgba(0,0,0,.04); }
.card-title { font-size: var(--fs-h2); font-weight: 700; color: var(--c-ink); margin-bottom: 16rpx; }
.stat-row { display: flex; justify-content: space-around; }
.stat { text-align: center; }
.num { font-size: 56rpx; font-weight: 800; color: var(--c-ink); display: block; }
.lbl { font-size: 24rpx; color: var(--c-text-hint); }
.row { display: flex; justify-content: space-between; padding: 8rpx 0; border-bottom: 1rpx solid var(--c-border); font-size: 26rpx; color: var(--c-text-body); }
.count { color: var(--c-gold); font-weight: 700; }
.sug { display: flex; margin-bottom: 16rpx; }
.sug-num { width: 44rpx; height: 44rpx; background: var(--c-primary); color: var(--c-ink); border-radius: 50%; font-size: 24rpx; font-weight: 700; line-height: 44rpx; text-align: center; margin-right: 16rpx; }
.sug-text { flex: 1; font-size: 28rpx; color: var(--c-text-body); line-height: 1.7; }
.tier-row { display: flex; gap: 16rpx; margin-bottom: 16rpx; }
.tier { flex: 1; text-align: center; padding: 16rpx 0; border: 2rpx solid var(--c-border); border-radius: var(--r-md); font-size: 24rpx; color: var(--c-text-body); }
.tier.active { border-color: var(--c-gold); background: var(--c-primary-faint); font-weight: 700; }
.btn-primary { background: var(--c-primary); color: var(--c-ink); border-radius: var(--r-btn); padding: 20rpx; font-weight: 700; font-size: 28rpx; }
.btn-primary[disabled] { background: var(--c-primary-soft); color: #b9a94e; }
</style>
```

- [ ] **Step 7: profile/index.vue 加 "家人" 卡片**

在教师中心卡之后、账号设置之前插入：
```vue
    <!-- 家人中心 -->
    <view class="card" @tap="goRelative">
      <view class="card-title">家人中心</view>
      <text class="menu-desc">学生：邀请家人绑定 · 家长：查看孩子学情、代付会员</text>
      <button class="btn-menu" @tap.stop="goRelative">进入家人中心</button>
    </view>
```
script 加：
```typescript
function goRelative() { uni.navigateTo({ url: '/pages/relative/center' }) }
```

家人中心入口同时承接"学生邀请家人"和"家长绑孩子" —— 在 center.vue 内可加 v-if 区分（但 MVP 简化：center.vue 主要是亲人侧；学生生成邀请码功能放到 profile 上加一个"邀请家人"按钮即可，简化）。

为了避免 center.vue 过复杂，**学生生成邀请码**入口在 profile/index.vue 用单独 toast/弹窗显示码：
```vue
    <view class="card">
      <view class="card-title">我的家人</view>
      <text class="menu-desc">生成邀请码，让父母/家人绑定到您的账号。</text>
      <button class="btn-menu" @tap="genInviteCode">生成邀请码</button>
      <view v-if="myInvite" class="invite-row">
        <text class="code">{{ myInvite.code }}</text>
        <text class="exp">24小时有效</text>
      </view>
    </view>
```
script 加：
```typescript
import { generateRelativeInviteCode } from '@/api/relative'
const myInvite = ref<{ code: string; expires_at: string } | null>(null)
async function genInviteCode() {
  try { const r = await generateRelativeInviteCode(); myInvite.value = r.data || null }
  catch (e: any) { uni.showToast({ title: e?.message || '生成失败', icon: 'none' }) }
}
```
样式：
```css
.invite-row { margin-top: 12rpx; background: var(--c-bg-soft); border-radius: var(--r-md); padding: 16rpx; display: flex; align-items: center; gap: 12rpx; }
.code { flex: 1; font-size: 36rpx; font-weight: 800; color: var(--c-ink); letter-spacing: 4rpx; }
.exp { font-size: 22rpx; color: var(--c-text-hint); }
```

- [ ] **Step 8: 提交前端**

```bash
cd /Users/johnlu/Desktop/ComeMoney/项目/ai-education/engGramer
git add frontend/miniprogram/src/types/api.ts \
        frontend/miniprogram/src/api/relative.ts \
        frontend/miniprogram/src/api/orders.ts \
        frontend/miniprogram/src/pages.json \
        frontend/miniprogram/src/pages/relative/ \
        frontend/miniprogram/src/pages/profile/index.vue
git commit -m "feat(relative): frontend — center + student-view + invite + relative-pay"
```

---

## Task 4: 集成验证 + D-076 + push

- [ ] **Step 1: 全量后端测试**

```bash
cd /Users/johnlu/Desktop/ComeMoney/项目/ai-education/engGramer/backend
python -m pytest ../tests/ -q
```
Expected: 220 PASS（206 + 6 task0 + 2 task1 + 2 task2 + ...）

- [ ] **Step 2: live server 冒烟，确认 /relative 端点**

```bash
uvicorn app.main:app --port 8028 --log-level warning &
UVICORN_PID=$!
sleep 3
curl -s http://localhost:8028/openapi.json | python3 -c "
import json,sys
spec = json.load(sys.stdin)
paths = sorted([p for p in spec['paths'].keys() if '/relative' in p])
print('Plan L 端点:')
for p in paths: print('  ', p)
print('count:', len(paths))
"
kill $UVICORN_PID 2>/dev/null || true
sleep 1
```
Expected: 7 个 /relative 端点

- [ ] **Step 3: 归档 D-076**

```markdown
## D-076｜亲人端 P0 三项补完：邀请绑定 + 查学情 + 代付会员

**日期：** 2026-05-29
**背景：** 学生端 + 老师端 + 合规两项 + 消息中心已完成。亲人端是小程序内能做的最后一块 P0，完成后小程序端 P0 全齐，剩余机构端/平台管理需独立 Web 后台。
**结论：**
1. **无新迁移：** `invite_code_type` 已含 `relative_bind`（D-061）；`StudentRelative` / `Relative` 表已存（域1）；`Order.payer_id` 和 `Order.beneficiary_id` 已分离 → 代付天然支持，仅在 API/请求 schema 加 `target_student_id`。
2. **邀请码绑定（学生发码、亲人扫码）：** `POST /relative/invite-code` 学生侧生成；`POST /relative/bind { code, relationship }` 亲人侧绑定；绑定成功创建 Relative 行 + user.role 升级为 "relative"。
3. **上限校验：** 每个学生最多 4 个 active 亲人（service 层 MAX_RELATIVES_PER_STUDENT 常量校验），第 5 个绑定 400。
4. **解绑：** 学生侧 `DELETE /relative/relatives/{relative_id}` set is_active=false + unbound_at，保留历史。
5. **查学情/错题：** `GET /relative/students/{id}/diagnosis-report` 和 `/wrong-questions`，前置 `assert_bound` 校验（active StudentRelative 必须存在）。
6. **代付会员：** orders POST 加 `target_student_id` 可选字段；非空时校验 active 亲人关系 → 创建 Order(payer=relative, beneficiary=student)；14-17 岁学生由亲人代付**自动写入 minor_purchase_consent_at**（§4.1 视为已获监护人同意），跳过 minor_consent 表单校验；membership 激活时挂到 beneficiary_id（学生）。
7. **前端：** 2 个新页 `pages/relative/center.vue`（绑定输入 + 我的孩子列表）和 `pages/relative/student-view.vue`（学情 + 三档会员选择 + 代付按钮）；profile 页加 "家人中心" 入口 + "生成邀请码" 入口（学生侧）。
8. **测试：** 10 个新测试，全量 ≥ 216 PASS。
**遗留：** 真实微信支付 sandbox 验证（dev 当前依赖 mock）；亲人接收"学生消费提醒"消息（推送授权流程未做）；亲人代付历史的可视化（订单列表筛选）；亲人解除自身全部绑定后 role 是否回退为 student（当前不回退）。
**影响范围：** 无新迁移；7 个新 API 端点 + orders POST 字段扩展；2 个新前端页 + profile 修改；测试 +10；小程序端 P0 全部完成；已推送 GitHub main 分支。

---
```

插入位置：`## D-075` 之前。

- [ ] **Step 4: 提交 + push**

```bash
git add "docs/决策归档.md"
git commit -m "docs: archive D-076 — relative end P0 complete (miniprogram P0 done)"
git push
```

---

## Self-Review

### Spec 覆盖
| P0 亲人端条目 | 实现 |
|---|---|
| 绑定学生账号（邀请码方式）| Task 0：generate + bind + max 4 + tests |
| 查看学情数据 | Task 1：diagnosis + wq endpoints |
| 代付会员 | Task 2：target_student_id + minor_consent 自动绕过 + activate 用 beneficiary |

### 类型一致性
- `StudentRelative.is_active` / `bound_at` / `unbound_at` 与 service / API / 测试一致
- `payer_id` 和 `beneficiary_id` 分离已存在于 Order 模型，本次新增的仅是 API 入参透传
- 14-17 岁亲人代付的 minor_consent 自动绕过逻辑与 D-073 §4.1 完全一致

### Placeholder
无 TBD；前端学生 ID 显示是 hex 前 8 字符（待真实数据后可换昵称）；账号注销与亲人 role 回退的边界已在归档明列。
