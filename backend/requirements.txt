websockets>=12.0
tencentcloud-sdk-python-aiart>=3.1.82

# 错误监控（可选；未配 SENTRY_DSN 时不加载）
sentry-sdk[fastapi]>=2.0.0

# 长难句句法复杂度分析（依存树:从句数/树深/MDD/难度分）
# 需另装英文模型: python -m spacy download en_core_web_sm
# spaCy 不可用时服务自动降级到信号词规则,不影响启动
spacy>=3.8.0

# 词形生成(考点·形态学打底 P1):时态/单复数/比较级确定性生成(离线自带数据);缺库时服务降级到规则表
lemminflect>=0.2.3

# 词汇关系校验(考点·WordNet P2):校验 LLM 近义/反义是否真实→升高置信;缺库/语料时服务降级(不升级)
# 需另下语料: python -m nltk.downloader wordnet omw-1.4
nltk>=3.8
